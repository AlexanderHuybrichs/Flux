apiVersion: v1
kind: Namespace
metadata: 
  name: Flux-webapp-development
---
kind: Service
apiVersion: v1
metadata:
  name: Flux-webapp-development
  namespace: Flux-webapp-development
spec:
  selector:
    app: Flux-webapp-development
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: Flux-webapp-development-deployment
  namespace: Flux-webapp-development
  labels:
    app: Flux-webapp-development
spec:
  replicas: 6
  selector:
    matchLabels:
      app: Flux-webapp-development
  template:
    metadata:
      labels:
        app: Flux-webapp-development
    spec:
      containers:
      - name: Flux-webapp-development
        image: alexanderh12/circleci:0.1.114
        ports:
        - containerPort: 80
